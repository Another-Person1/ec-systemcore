# Dedicated wired EtherCAT interface. No default: never claim the robot network.
# Prefer a dedicated USB Ethernet adapter; identify its interface with ip link.
# Remove this interface from DHCP/IP/bridge management before starting.
INTERFACE=""
